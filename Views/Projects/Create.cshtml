@model WebWorkNew.Models.Project

@{
    ViewData["Title"] = "Создание проекта";
}

<h2 class="mb-4">Создание нового проекта</h2>

<div class="card">
    <div class="card-header bg-primary text-white">
        <h5 class="mb-0">Форма создания проекта</h5>
    </div>

    <div class="card-body">
<form asp-action="Create" method="post" autocomplete="off">
            @Html.AntiForgeryToken()
            <div class="row">
                <div class="col-md-8">
                    <div class="form-group mb-3">
                        <label asp-for="Title" class="control-label">Название проекта *</label>
                        <input asp-for="Title" class="form-control" />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group mb-3">
                                <label asp-for="StartDate" class="control-label">Дата начала *</label>
                                <input asp-for="StartDate" type="date" class="form-control" />
                                <span asp-validation-for="StartDate" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group mb-3">
                                <label asp-for="EndDate" class="control-label">Дата окончания *</label>
                                <input asp-for="EndDate" type="date" class="form-control" />
                                <span asp-validation-for="EndDate" class="text-danger"></span>
                            </div>
                        </div>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Description" class="control-label">Описание</label>
                        <textarea asp-for="Description"
                                  class="form-control"
                                  rows="6"
                                  data-tinymce="true"
                                  placeholder="Введите описание проекта..."></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="CustomerId" class="control-label">Заказчик</label>
                        <select asp-for="CustomerId" class="form-select" asp-items="ViewBag.Customers as SelectList">
                            <option value="">-- Выберите заказчика --</option>
                        </select>
                        <span asp-validation-for="CustomerId" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="TaxRate" class="control-label">Налоговая ставка (%) *</label>
                        <input asp-for="TaxRate" class="form-control" type="number" step="0.01" />
                        <span asp-validation-for="TaxRate" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Status" class="control-label">Статус</label>
                        <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<WebWorkNew.Models.ProjectStatus>()">
                        </select>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header bg-primary text-white">
                            <h5 class="mb-0">Информация</h5>
                        </div>
                        <div class="card-body">
                            <p class="mb-2"><strong>Создается:</strong> @DateTime.Now.ToString("dd.MM.yyyy HH:mm")</p>
                            <p class="mb-2"><strong>Пользователь:</strong> @User.Identity?.Name</p>

                            <hr />

                            <div class="alert" style="background: rgba(255,255,255,.06); border: 1px solid rgba(255,255,255,.12); color: rgba(234,242,255,.85);">
                                <i class="bi bi-info-circle"></i>
                                После создания проекта вы сможете добавить ресурсы.
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3" style="display:flex; gap:10px; flex-wrap:wrap; align-items:center;">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Создать проект
                </button>
                <a asp-action="Index" class="btn btn-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

